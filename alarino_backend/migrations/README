Single-database configuration for Flask.

#### Database setup
```
# export FLASK_APP=main:create_app
# flask --app=main:create_app routes
# flask --app=main:create_app db init - only run once to setup the actual database
# flask --app=main:create_app db migrate -m "commit message"
# flask --app=main:create_app db upgrade

```
